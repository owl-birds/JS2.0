import { renameSync } from "fs";

export const rename_filenames_sync = (
  //
  old_filename_path_list: string[],
  new_filename_path_list: string[]
) => {
  //
  try {
  } catch (error) {
    throw new Error("Error at renaming files");
  }
};
